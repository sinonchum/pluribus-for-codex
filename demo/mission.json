{
  "repository": "generated by demo/start-demo.mjs",
  "objective": "Add GET /health/details with version, uptime, and sanitized database status. Reuse the existing HealthService, add one degraded-state test, and do not change authentication or add dependencies.",
  "roles": ["scout", "builder", "tester", "reviewer"],
  "max_concurrency": 2,
  "protected_paths": [".env", ".git/**", "src/middleware/auth.js"],
  "verification_commands": [{ "executable": "npm", "args": ["test"], "timeout_seconds": 120 }]
}
